# Bu fayldan nusxa olib, .env nomli yangi fayl yarating.
# Create a new file named .env by copying this file.

# --- Asosiy Sozlamalar ---
# --- Main Configuration ---

# O'zingizning vast.ai instance'ingizning IP manzilini shu yerga qo'ying.
# Put the IP address of your vast.ai instance here.
VITE_INSTANCE_IP="127.0.0.1"


# --- Qo'shimcha Sozlamalar (Odatda o'zgartirish shart emas) ---
# --- Advanced Configuration (Usually no need to change) ---

# Agent (buyruq yuborish uchun) ishlayotgan port.
# The port the agent (for sending commands) is running on.
VITE_AGENT_PORT=5001

# Stream (video oqim uchun) ishlayotgan port.
# The port the stream (for the video feed) is running on.
VITE_STREAM_PORT=8080

# (Ixtiyoriy) Stream URL'ining qolgan qismi (token va boshqa parametrlar uchun).
# (Optional) The rest of the stream URL path (for tokens and other parameters).
# Agar sizda tokenli URL bo'lsa, shu yerga "/?token=..." kabi qismini yozing.
# If you have a URL with a token, write the "/?token=..." part here.
VITE_STREAM_PATH_AND_QUERY="/?ui=none"
